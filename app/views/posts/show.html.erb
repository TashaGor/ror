<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>
<p>
  <strong>Категории:</strong>
  <%= @post.categories_titles %>
</p>
<p>
  <strong>Body:</strong>
  <%= @post.body %>
</p>

<%= render 'comments/comments', object: @post %>
<%= render 'comments/form' %>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
